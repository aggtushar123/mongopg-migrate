"""mongopg-migrate: map MongoDB collections onto an existing, independently
designed PostgreSQL schema and run a validated, repeatable migration.

See PRD-mongo-postgres-migration-tool.md at the repo root for the product
spec this package implements against — module docstrings reference PRD
section numbers (e.g. "PRD §12") so implementation and spec stay traceable.
"""

__version__ = "0.1.0"
